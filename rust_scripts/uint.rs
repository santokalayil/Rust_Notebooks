fn main() {
    // Unsigned integer is nomrally used when 
    println!(" THE UNSIGNED INTEGER Is used if I am sure that no negative numbers will be used...");
    let x : u32 = 4999;
    println!(" The Value of x = {}", x);

    let f: f32 = 4.5; // float 32 bit;
    let b: bool = false; // boolean
    println!("First is a float and the next is a boolian ---- {}   {}", f, b);
}
